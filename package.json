{
  "type": "module",
  "name": "student-grades",
  "version": "1.0.1",
  "main": "./src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel ./src/index.html --open",
    "prebuild": "rm -rf dist .cache",
    "build": "parcel build ./src/index.html --public-url ./",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zkinsk/grade-search.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/zkinsk/grade-search/issues"
  },
  "homepage": "https://github.com/zkinsk/grade-search#readme",
  "description": "",
  "dependencies": {
    "parcel-bundler": "^1.12.5"
  },
  "devDependencies": {
    "@types/jquery": "^3.5.5",
    "gh-pages": "^3.1.0",
    "typescript": "^4.2.4"
  }
}
